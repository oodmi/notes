version: '3.8'

services:
  mongodb:
    image: mongo
    restart: always
    container_name: mongo-test
    ports:
      - '27017:27017'
    volumes:
      - dbdata6:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password

volumes:
  dbdata6: